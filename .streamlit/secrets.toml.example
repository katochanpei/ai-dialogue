# Streamlit Cloud デプロイ時に設定する secrets のテンプレート。
# 実ファイルは `.streamlit/secrets.toml` で、.gitignore で除外されます。
#
# ローカルでこのファイルを使う場合は:
#   cp .streamlit/secrets.toml.example .streamlit/secrets.toml
# して値を埋める。
#
# Streamlit Cloud では https://share.streamlit.io のアプリ設定画面で
# 同じ内容を貼り付ける。

GEMINI_API_KEY = "your-gemini-api-key-here"
APP_PASSWORD = "your-app-password-here"

# 🕵️ 管理者パスワード（オプション）
# このパスワードでログインすると、MULTI_USER_MODE でも過去ログ一覧が見える。
# 同僚にはバレんように、別の値にしとくこと。
ADMIN_PASSWORD = "your-admin-password-here"

# クラウド共有モード（複数人で使う時）。
# true にすると:
#   - 一般ユーザーのサイドバーから過去ログ一覧を非表示
#   - 管理者パスワードでログインした場合のみ閲覧可
# ローカル単独で使うなら false / 未設定でOK
MULTI_USER_MODE = "true"
